import { SqtrValuePipe } from './sqtr-value.pipe';

describe('SqtrValuePipe', () => {
  it('create an instance', () => {
    const pipe = new SqtrValuePipe();
    expect(pipe).toBeTruthy();
  });
});
