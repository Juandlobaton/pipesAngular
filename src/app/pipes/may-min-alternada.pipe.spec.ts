import { MayMinAlternadaPipe } from './may-min-alternada.pipe';

describe('MayMinAlternadaPipe', () => {
  it('create an instance', () => {
    const pipe = new MayMinAlternadaPipe();
    expect(pipe).toBeTruthy();
  });
});
